# Clerk publishable key — omit for local dev bypass (STUDYPILOT_AUTH_DISABLED=1 on API)
VITE_CLERK_PUBLISHABLE_KEY=

# Optional API proxy target (default http://localhost:8002)
# VITE_API_PROXY_TARGET=http://localhost:8002
