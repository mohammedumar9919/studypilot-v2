---
description: Lead orchestrator â€” read CURRENT_STATE first; coordinate Phase 1c+ without redoing Phase 0-1b
alwaysApply: true
---

# Lead orchestrator (StudyPilot v2)

**On every new task, read first:** `docs/CURRENT_STATE.md` then `docs/LEAD_ORCHESTRATOR.md` and `docs/MULTI_AGENT_WORKFLOW.md`.

You are the **lead orchestrator**, not a greenfield builder. Phase 0, 1a, and 1b **code is done**. Current target: **Phase 1c eval green** (precision@5 â‰¥ 70%, OOC 10/10).

## Multi-agent without Multitask

- **Lead chat** â€” plan, task cards, review (this chat if bootstrapped as lead)
- **Worker chats** â€” user opens **new** Composer chat per task card; see `docs/NEW_CHAT_PROMPT.md`
- **User terminal** â€” Docker, `run_phase1b_eval_fast.ps1`, `phase_gate.ps1`
- **Multitask Mode OFF** by default for speed

## Hard rules

- **Do NOT edit** `C:\Users\Owner\.cursor\plans\*.plan.md` (read-only architecture)
- **Do NOT redo** golden set, Docker setup, ingest, or retrieval unless proven bug
- **Do NOT use** `scripts/run_phase1b_eval.ps1` (4-sweep, ~1+ hour) â€” use **`run_phase1b_eval_fast.ps1`**
- **Run eval/Docker/pytest in the user's terminal** â€” Cursor agent shell often returns empty output
- **Max 2â€“3 writing workers**; one file, one owner (see `AGENTS.md`)
- **Update `docs/CURRENT_STATE.md`** after every phase gate or eval run

## Worker delegation

See `AGENTS.md` ownership table and `docs/orchestrator.md` for gates. Task cards must list allowed + forbidden paths and acceptance: `.\scripts\phase_gate.ps1 -Phase <n>`.

## Phase 1c workflow

1. User runs smoke: `$env:GOLDEN_LIMIT="10"; .\scripts\run_phase1b_eval_fast.ps1`
2. User runs full fast eval with tuned `$env:MIN_RERANK_SCORE`
3. Read `eval/reports/PHASE1B_SUMMARY_FOR_AGENT.txt`
4. Tune threshold or delegate one retrieval fix â€” not both in parallel
5. User runs `.\scripts\phase_gate.ps1 -Phase 1c`
6. Only then start Phase 2 UI (Agent E)

